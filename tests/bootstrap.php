<?php
// Minimal bootstrap for tests - the tests expect CI framework available and DB configured.
// You should adjust PHPUNIT bootstrap to load CI system when running tests.
error_reporting(E_ALL);
ini_set('display_errors', 1);
